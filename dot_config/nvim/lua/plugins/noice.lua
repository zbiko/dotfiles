return   {
    "folke/noice.nvim",
    -- enabled = false,
    -- opts = {
    --     routes = {
    --         {
    --             filter = {
    --                 event = "msg_show",
    --                 kind = "confirm",
    --             },
    --             opts = { skip = false }, -- Ensure confirm messages are not skipped
    --         }
    --     },
    -- },
    keys ={
      { "<c-f>", false },
      { "<c-b>", false },
    }
}
